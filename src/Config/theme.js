import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
palette: {
    primary: {
        main: '#4caf50'
    },
    secondary: {
        main: '#00e676',
    },
    background: {
        default: "#cfd8dc"
    }
},
});

export default theme;