import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
palette: {
    primary: {
        main: '#E85A4F'
    },
    secondary: {
        main: '#00e676',
    },
    background: {
        default: "#EAE7DC"
    }
},
});

export default theme;